# name: Daily Data Fetch & Retrain

# on:
#   schedule:
#     # Runs at 06:00 UTC every day
#     - cron: '0 6 * * *'
#   # Allows you to run it manually from GitHub for testing
#   workflow_dispatch:

# jobs:
#   update-brain:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write  # Give permission to push changes back to the repo

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.10'

#       - name: Install Dependencies
#         run: |
#           pip install -r requirements.txt

#       - name: Fetch New Data
#         run: |
#           echo "🌐 Fetching latest market prices..."
#           python src/data/fetch_data.py

#       - name: Retrain Model
#         run: |
#           echo "🧠 Retraining the AI model..."
#           python src/models/train.py

#       - name: Commit and Push Changes
#         run: |
#           git config --global user.name "Kestrikah Bot"
#           git config --global user.email "bot@kestrikah.com"
          
#           # Check if anything changed
#           if [[ -n $(git status -s) ]]; then
#             echo "📦 Changes detected. Pushing new brain..."
#             git add data/ models/
#             git commit -m "Daily Auto-Update: New Data & Retrained Model"
#             git push
#           else
#             echo "💤 No changes in data. Skipping push."
#           fi
